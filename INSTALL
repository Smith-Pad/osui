################################################################################
##
##			OSUI INSTALLATION SCRIPT
##
##
##
################################################################################



cd /var/                                                ## Change to the /var directory 
git clone https://github.com/Smith-Pad/osui/            ## This is where we clone the repository.
cd osui                                                 ## Change the Directory to osu
cp openbox /etc/xdg/                                    ## Copy the openbox config to /etc/xdg/
cd systemd/                                             ## Go to the systemd directory
cp -R admin-interface.service /etc/systemd/system       ## Copy the systemd service to /etc/systemd/system
cd ..
cd sbin/                                                ## Go to the sbin directory
cp -R admin-interface.sh /usr/sbin                      ## Copy the script to /usr/sbin
sudo systemctl enable admin-interface.service           ## Enable the service 
echo """
#################################################################
#              /!\ IT IS BEST TO REBOOT YOUR SYSTEM
#################################################################

"""